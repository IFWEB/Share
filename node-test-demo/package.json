{
  "name": "mocha-demo",
  "version": "0.0.0",
  "main": "app.js",
  "scripts": {
    "start": "set NODE_ENV=development && node app.js",
    "test": "set NODE_ENV=test && mocha",
    "istanbul":"set NODE_ENV=test && nyc --reporter=html --reporter=text mocha"
  },
  "dependencies": {
    "bcrypt": "^1.0.3",
    "body-parser": "^1.18.2",
    "cookie-parser": "^1.4.3",
    "express": "^4.15.5",
    "express-validator": "^4.2.1",
    "jsonwebtoken": "^8.0.1",
    "mongoose": "^4.11.7"
  },
  "devDependencies": {
    "mocha": "^3.5.3",
    "nyc": "^11.2.1",
    "should": "^13.1.0",
    "supertest": "^3.0.0"
  }
}
